add_newline = true

[character]
success_symbol = "🍡"
error_symbol = "👹"

[time]
disabled = false
time_format = "%m%dT%H:%M:%S"
format = "🕘[$time]($style)"

[directory]
truncation_symbol = ".../"

[git_branch]
symbol = "🌱"

[gcloud]
format = "$directory[$symbol$active]($style)"

[golang]
format = "$directory[$symbol$version](bold cyan)"

